import {StyleSheet} from 'react-native'

const styles = StyleSheet.create({
    container: {
        flexDirection: 'row',
        justifyContent: 'space-between',
        marginTop: 20
    },
    item: {
        paddingHorizontal: 16,
        paddingVertical: 16,
    },
    name: {fontSize: 17, color: '#495358', fontWeight: '500'}
})

export default styles
